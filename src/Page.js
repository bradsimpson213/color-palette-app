// React imports
import React from 'react';

const Page = ({ children }) => {
    return (
        <section className="page">
            { children }
        </section>
    )
};

export default Page;